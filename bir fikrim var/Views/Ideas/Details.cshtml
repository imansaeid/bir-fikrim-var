@model IdeaDto

@{
    ViewData["Title"] = "Idea Details";
}

<h2>@Model.Title</h2>
<p>@Model.Content</p>
<p>
    <small>Created: @Model.CreatedDate?.ToString("g")</small>
</p>

<a asp-controller="Ideas" asp-action="Index" class="btn btn-secondary">Back to Feed</a>
